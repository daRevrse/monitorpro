{
  "name": "monitorpro",
  "version": "1.0.0",
  "private": true,
  "description": "MonitorPro - lanceur local (backend + frontend)",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c magenta,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend start\"",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
